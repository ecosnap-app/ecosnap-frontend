# Copia este archivo a .env y llena los valores.
# El archivo .env NO se sube a GitHub (está en .gitignore).
#
# Sprint 3: puedes dejarlos vacíos — la app corre en modo demo.
# Sprint 4: llénalos y la app empieza a usar Supabase de verdad.

# Supabase -> Project Settings -> API
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# URL del backend desplegado en Render (Sprint 5)
EXPO_PUBLIC_API_URL=
